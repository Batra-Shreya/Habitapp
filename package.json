{
  "name": "habitat",
  "version": "1.0.0",
  "description": "A friendly habit tracker with rewards and a live accountability buddy. Zero dependencies.",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node test/api.test.js",
    "icons": "node tools/make-icons.js",
    "check": "node --check server/server.js && node --check server/store.js && node --check public/app.js && node --check public/api.js && node --check public/service-worker.js && node --check tools/make-icons.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
